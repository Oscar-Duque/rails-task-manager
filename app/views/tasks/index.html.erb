<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %> - <%= task.details %> - Completed: <%= task.completed %> <%= link_to 'delete', task_path(task), method: :delete %></li>
  <% end %>
</ul>